<button
  class="primary-button {{block ? 'primary-button--block' : ''}} {{disabled ? 'primary-button--disabled' : ''}}"
  loading="{{loading}}"
  disabled="{{disabled || loading}}"
  bindtap="handleTap"
>
  {{text}}
</button>
